# IP01E follow-up — Formation internal headings

The registered-information title and four chip-category titles (Certificaciones,
Cursos, Diplomados, Miembro de) receive a shared presentation class. Although the
four category titles are existing label elements, they serve as the explicitly
requested structural block titles. Their elements and behavior are retained.
Ordinary credential/data labels and the summary label are not targeted.

The CSS shares the parent heading's #06AEB8, 1.25rem (20px), 700 weight and icon
rule (2.5em). None of the five targets has an icon, and no icons are added.
Existing margins, fields, chips, buttons, arrangement and behavior remain.

Read-only browser QA verifies all five computed styles at 1440×900, 1366×768,
820×1180 and 390×844. No overflow or runtime exceptions; screenshots confirm
readable compact wrapping. Artifacts: /tmp/ip01e-followup/. No data writes.
The pending tab trial remains excluded and untouched.
